#$.fn.getFileName = ->
#	fileName = @val()
#	if matches = fileName.match(/(.+\\)?(.+)$/)
#		fileName = matches[2] or fileName
#	fileName

##= require jquery
##= require jquery_ujs
#
#$(document).on 'click', '.js-link', (event) ->
#	target = $(event.currentTarget).data
#	cout 'data', target
#	$field.prev('label').text $filed.getFileName()
